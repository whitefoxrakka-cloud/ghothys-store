RELAY GHOTHYS STORE — CATATAN DEPLOY & KEAMANAN
================================================

WORKER AKTIF
  Nama   : empty-snow-7e64ghothys-notif
  URL    : https://empty-snow-7e64ghothys-notif.whitefox-rakka.workers.dev
  File   : relay/wrangler.toml  (nama worker di sini WAJIB sama dengan di dashboard)
  Deploy : npx wrangler deploy --keep-vars
           WAJIB --keep-vars, kalau tidak semua variabel dashboard ikut terhapus.

SECRET SUDAH TERPASANG (type secret, terenkripsi)
  SECRET                    kata sandi untuk tulis konten (X-Ghothys-Secret)
  AIRTABLE_PAT              Personal Access Token Airtable
  AIRTABLE_BASE_ID_SECRET   ID base Airtable
  TELEGRAM_BOT_TOKEN        token bot Telegram
  OWNER_EMAIL               email owner untuk Cloudflare Access

VARIABEL PUBLIK (plain, aman tampil di wrangler.toml)
  AIRTABLE_CONTENT_TABLE_NAME = "Content"
  ALLOWED_ORIGINS = "https://whitefoxrakka-cloud.github.io,http://127.0.0.1:8765,http://localhost:8787"

MASIH PLAIN VARIABLE (nilai hanya di dashboard, belum dienkripsi)
  DISCORD_WEBHOOK_URL   -> kalau mau dienkripsi, jalankan:
                            npx wrangler secret put DISCORD_WEBHOOK_URL_SECRET
                          lalu worker otomatis memakai nilai *_SECRET (lihat cfgValue)
  TELEGRAM_CHAT_ID      -> npx wrangler secret put TELEGRAM_CHAT_ID_SECRET
  AIRTABLE_BASE_ID      ->variabel lama, sudah digantikan AIRTABLE_BASE_ID_SECRET
                            (boleh dihapus di dashboard, tidak wajib)
  Worker memakai pola: env.NAMA_SECRET || env.NAMA, jadi nama *_SECRET diprioritaskan.

ENDPOINT
  GET  /content                      publik, baca konten owner
  GET  /order-status?order_id=INV-   publik, cek status pesanan
  POST /content                      WAJIB X-Ghothys-Secret (atau Cloudflare Access)
  POST / (order/notify)              WAJIB X-Ghothys-Secret (atau Cloudflare Access)

ATURAN WAJIB
  1. Selalu deploy dengan --keep-vars.
  2. Jangan pernah print nilai secret ke terminal/chat.
  3. Untuk akses dari domain lain, tambahkan origin-nya ke ALLOWED_ORIGINS,
     lalu deploy ulang (var dashboard tidak otomatis berubah).

ROLLBACK
  npx wrangler rollback <version-id-sebelumnya>
  Versi sebelum hardening CORS/Access: 88517fa8-2668-4298-a81f-53f1ce495352
